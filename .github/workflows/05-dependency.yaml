name: Dependency
on:
  push:
    paths:
      - '.github/workflows/05-dependency.yaml'
jobs: # Refers Pipelines
  job-1: # Refers job name
    runs-on: ubuntu-latest # Refers the Platform where the job should be execute
    steps:
      - name: Step1
        run: echo " Hi, This is from Job 1"
  
  job-2:
    needs: job-1
    runs-on: ubuntu-latest
    steps:
      - name: Step1
        run: echo "Hi, Thi is from Job 2"

  job-3:
    needs: job-2
    runs-on: ubuntu-latest
    steps:
      - name: Step3
        run: echo "Hi, This is from Job 3"

  job-4:
    runs-on: ubuntu-latest
    steps:
      - name: Step4
        run: echo "Hi, This is from Job 4"

  job-5:
    needs: job-3
    runs-on: ubuntu-latest
    steps:
      - name: Step5
        run: echo "Hi, this is from Job 5"

  job-6:
    needs: job-5
    runs-on: ubuntu-latest
    steps:
      - name: Step6
        run: echo "Hi, this is from Job 6"
